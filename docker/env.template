# Confluence access token
FGC_CONFLUENCE_TOKEN=asdfasdf

# URL for database
DB_URL=sqlite:////db/sqlite.db

# Location of database files to be mounted RW, if using file access.
DB_DIR=/db

# TLS certificate
CERT_FILE=$PWD/certs/cert.crt
KEY_FILE=$PWD/certs/cert.key
